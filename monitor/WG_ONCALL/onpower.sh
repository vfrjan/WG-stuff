#!/bin/bash
#set -x
echo -e "Main UPS(EMERSON) running on power again. Power probably restored in G.B. 3 `date`" >> /WG_ONCALL/ups-message
echo -e "Main UPS(EMERSON) running on power again. Power probably restored in G.B. 3" >> /WG_ONCALL/outage-message
